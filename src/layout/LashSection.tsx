import React from "react";
import withTransition from "../utils/withTransition";
type LashSectionProps = {};

const LashSection = (props: LashSectionProps) => {
  return <h1>LashSection</h1>;
};

export default withTransition(LashSection);
