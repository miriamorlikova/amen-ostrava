import React from "react";
import withTransition from "../utils/withTransition";
type WorkSectionProps = {};

const WorkSection = (props: WorkSectionProps) => {
  return <h1>WorkSection</h1>;
};

export default withTransition(WorkSection);
