import React from "react";
import withTransition from "../utils/withTransition";

type BrowSectionProps = {};

const BrowSection = (props: BrowSectionProps) => {
  return <h1>BrowSection</h1>;
};

export default withTransition(BrowSection);
